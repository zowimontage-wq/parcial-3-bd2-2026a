# Copia este archivo como  .env  y pon tus credenciales reales de MySQL.
# El archivo .env NO se sube al repositorio (está en .gitignore).
DB_HOST=host.docker.internal
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=cine
SECRET_KEY=cambia-esta-clave
